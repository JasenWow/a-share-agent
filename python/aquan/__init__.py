"""aquan — A-share quantitative analysis system.

Public layer for the Python side of the aquan monorepo. Contains:
- aquan.core    — domain types, errors, config, contracts
- aquan.utils   — pure utilities (io, dates, http, logging, testing)
- aquan.metrics — quant metric library (compile_query, list_metrics, ...)
- aquan.cli     — `aquan` command-line entry point
"""

__version__ = "0.1.0"
