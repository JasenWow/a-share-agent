[project]
name = "prediction-store-mcp-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "pandas>=2.0",
    "fastapi>=0.110",
    "uvicorn>=0.27",
    "akshare>=1.10",
]

[project.scripts]
prediction-store-mcp = "server:main"