[project]
name = "bilibili-mcp-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "pandas>=2.0",
    "fastapi>=0.110",
    "uvicorn>=0.27",
    "bilibili-api-python>=16.0",
]

[project.scripts]
bilibili-mcp = "server:main"
